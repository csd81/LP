Problem 43.
    Solve the shortest path problem on an arbitrary simple weighted graph, specified as follows. Given two nodes, find a path connecting these two nodes so that the total weight of edges on the path is minimal. 

